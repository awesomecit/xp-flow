services:
  test:
    image: node:22-alpine
    working_dir: /app
    volumes:
      - .:/app
      - /app/node_modules
    command: npm test

  xpflow:
    build: .
    volumes:
      - ./.xpflow:/app/.xpflow:rw
    profiles: [run]
    command: status
